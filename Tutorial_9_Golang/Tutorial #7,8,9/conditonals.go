package main

import "fmt"

func main() {

	//Condicionales
	/*x := "hol"
	y := "Hol"
	val := x == y //Ambos lados deben ser del mismo tipo
	fmt.Printf("%t\n", val)*/

	/*Operadores
	// ! -> not
	// || -> or
	// && -> and

	val := 5 < 7 || 7 > 9

	fmt.Printf("%t\n", val)*/

	//If-else if-else
	age := 18

	if age > 9 {
		fmt.Println("a")

	} else if age < 100 {
		fmt.Println("b")

	} else {
		fmt.Println("c")
	}
}
